# Copy this file to `.env` in the same folder (frontend/.env) and fill in
# the values from your Supabase project settings. See ../SUPABASE_SETUP.md
# for the full step-by-step guide.
#
# Supabase Dashboard -> Project Settings (gear icon) -> API Keys

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
