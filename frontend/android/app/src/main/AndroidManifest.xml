<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Wajib di Android 13+ (API 33) supaya notifikasi push (FCM) bisa muncul. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Tanpa ini, fitur scan barcode di POS (pakai getUserMedia() lewat kamera)
         SELALU gagal/ditolak di APK Android — beda dengan browser biasa, WebView
         di dalam APK tidak akan pernah bisa dapat izin kamera kalau permission-nya
         tidak dideklarasikan di sini, walaupun user pencet "Izinkan" di dialog
         yang muncul (dialognya bahkan mungkin tidak akan muncul sama sekali). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- required="false" supaya app tetap bisa diinstall di device tanpa kamera
         (Play Store tidak akan menyembunyikan app dari device tanpa kamera). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
</manifest>
